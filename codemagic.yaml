workflows:
  android-workflow:
    name: Open The Path Legacy Custom Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      # قمنا بتغيير الإصدار إلى نسخة مستقرة أقدم تتوافق مع الأكواد المستعملة في مشروعك
      flutter: 3.10.6
      xcode: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
    scripts:
      - name: Force Clear Cache & Clean Project
        script: |
          rm -rf android/.gradle
          rm -rf ~/.gradle/caches
          flutter clean
      - name: Configure Java Environment & Local Properties
        script: |
          echo "flutter.sdk=$FLUTTER_ROOT" > android/local.properties
          echo "sdk.dir=$ANDROID_SDK_ROOT" >> android/local.properties
      - name: Fetch Flutter Dependencies
        script: |
          flutter pub get
      - name: Execute Autonomous Android Release Build
        script: |
          flutter build apk --release --no-pub
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
